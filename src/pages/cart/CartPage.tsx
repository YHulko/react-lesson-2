import { Typography } from '@mui/material'

type Props = {}

const CartPage = (props: Props) => {
    return (
        <div>
            CartPage
            <Typography
                variant="h4"
                component="h1"
                sx={{ margin: '30px 0' }}
            ></Typography>
        </div>
    )
}

export default CartPage
