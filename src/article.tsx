type Props = {
    title: string
    description: string
    autor: string
}

const Article = ({ title, description, autor }: Props) => {
    return (
        <div>
            <h2> className=""</h2>
        </div>
    )
}
